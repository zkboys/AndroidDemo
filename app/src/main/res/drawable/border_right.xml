<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This is the line -->
    <item>
        <shape>
            <solid android:color="@color/borderColorPrimary"/>
        </shape>
    </item>

    <!-- This is the main color -->
    <item android:right="1dp">
        <shape>
            <solid android:color="@color/colorPrimary"/>
        </shape>
    </item>
</layer-list>